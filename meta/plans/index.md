# Plans

Intended work on the brain or its tooling — design/decision records for proposed
changes, each capturing motivation, the shape of the change, scope boundaries, and
open questions so a future session can execute against something concrete. A separate
namespace from `issues` (problems to track) and `policy` (the rules that compile into
the contract).

Each plan is a `type: plan` doc carrying a `status` (`proposed` · `accepted` ·
`in-progress` · `done` · `superseded`). Superseded and done plans stay filed as a
record of what was decided and why. Entries within each section are ordered by
`timestamp`, most recent first (see the
[collection-view-by-date plan](/meta/plans/collection-view-by-date.md)).

## Proposed

- [Decision extraction at capture + a compiled decision graph over the brain's history](/meta/plans/decision-extraction-and-compiled-decision-graph.md) — close the two gaps the [decision-records analysis](/meta/analysis/decision-records-as-history-abstraction.md) found (undistilled rationale for unrouted decisions; no connected lineage view): `/capture` mints per-decision records (statement, alternatives declined, distilled rationale, links) beside the routing ledger, typed edges (`implements`/`supersedes`/`refines`) key on stable ids, and `mix brain.decisions` materializes a CI-`--check`-gated decision tree rendered on the Pages site — a derived view in the route-tags mold, never a new authored history layer. Direction operator-endorsed 2026-07-20; record shape, storage location, and backfill await ratification. `status: proposed`.
- [A thin Jido-based host that treats the brain as its world](/meta/plans/thin-jido-brain-host.md) — build the owned automation host the runtime-evaluation analyses have been circling: a separate, modern-toolchain Elixir/OTP application (sibling to this repo) where Jido supplies the chassis and the novel work is the harness semantics that make a hosted agent a contract-obeying session — CLAUDE.md as instructions, SKILL.md progressive-disclosure loading, bundle tools over a checkout, `brain.*` verbs as Actions, and a PR-only write-gatekeeper — with eve's host anatomy as the shape, two of the three standing blockers dissolved by the sibling-app boundary, activation still gated on the standing triggers, and the broken daily `/research` run as the first workload. `status: proposed`.
- [Library spin-out: knowledge-base repos consuming Elixir Mind as a packaged dependency](/meta/plans/library-spin-out-and-dependency-distribution.md) — the executable spec Phase 4 of the bundle/library separation plan deferred: the library extracts into its own repo — named **composable-beliefs-3** (operator-decided; this repo keeps elixir-mind), history-filtered, with a `cb:`-namespaced demo/fixture bundle and the config surface Phase 3 audits — while this repo sheds the code and remains the knowledge base, keeping its `pr:` anchors and cited SHAs reachable, consuming the library as a versioned Mix dependency (git-tag pin first, Hex publication once the API stabilizes and the go-public call is made). Id namespaces are per-bundle (`em:` here, `cb:` in the demo bundle — identity never transfers across bundles); config becomes the per-bundle manifest (id prefix, vocabularies, excluded dirs, URLs); CI shrinks to a reusable workflow; editorial skills stay per-bundle. `status: proposed`.
- [Swarm-eval harness: an Inkling-backed OTP application for measuring agent-swarm dynamics and failure modes](/meta/plans/inkling-beam-swarm-eval-harness.md) — build the eval harness the [inkling-beam-swarm-eval-substrate analysis](/meta/analysis/inkling-beam-swarm-eval-substrate.md) mapped: a separate modern-toolchain Elixir/OTP application (not this repo, not a brain runtime) driving deterministic self-hosted Inkling-Small inference through fault-scheduled swarm rollouts over the corpus-maintenance task domain, with Tinker across a JSONL-and-adapter file boundary. Executed strictly rung by rung on the M0–M5 ladder — determinism canary, single-agent baseline, concurrent writers, fault injection, population/effort sweeps, RL loop closure — each rung falsifiable before the next is funded; only M0 is committed by acceptance, and M0–M2 need no fine-tuning. `status: proposed`.
- [Derived index listings: compile Contents sections from frontmatter, with write-time summaries](/meta/plans/derived-index-listings.md) — retire hand-maintained `index.md` listings: `mix brain.index` derives every directory's listing section from its children's frontmatter (newest-first, glossary A–Z, status-grouped where children carry `status`), bullet text from a new write-time `summary` field with `description` fallback; absorbs PR #70's nav-sort core, supersedes the stale sibling PRs #68 (since closed and recreated as merged PR #95) and #70, and records the LLM-placement decision rule (agents write, compilers derive, checkers compare). `status: proposed`.
- [Separate the two concerns: the OKF knowledge bundle and the Elixir Mind library](/meta/plans/separate-okf-bundle-and-elixir-mind-library.md) — delineate the repo's two conflated deliverables: the personal knowledge bundle (an opinionated OKF v0.1 collection carrying the Elixir Mind metadata schema) and the Elixir Mind library (the Elixir code and everything not knowledge-document-related, plus the schema it defines and enforces). Doc-first: README/preamble/root-index reframing and a bundle-library-separation doctrine now; schema-as-artifact, a configurability audit, and the eventual library spin-out (own repo, demo bundle, this repo as a consuming collection) as deferred phases. `status: proposed`.
- [Concept terminology: the document/concept split (done) and the concept-type redefinition (open)](/meta/plans/concept-terminology-and-type-redefinition.md) — Part 1 (done): adopt **document** for the OKF unit, reserve bare **`concept`** for the type, fixing the routing-ledger collision — ratified/executed in the 2026-07-13 session (PR 71, unmerged) and re-landed on current `main` 2026-07-15. Part 2 (open): the `concept` *type* is two speech acts under one name (term definition vs accepted proposition) under an "(established/accepted)" label nothing enforces; ends in operator-ratification questions (own type for glossary terms? drop claim→concept graduation? drop or enforce the parenthetical? plus the deferred code-side vocabulary sweep). `status: proposed`.
- [The belief layer: a `belief` type and a real /beliefs/ namespace](/meta/plans/belief-type-and-beliefs-namespace.md) — introduce a `belief` type for operator-held, value-laden decision priors (outside the verification ladder, citable like doctrine, parallel to it rather than beneath it) and make `/beliefs/` its actual home; adds the epistemic/teleological filing test to the vocabulary and explicitly bounds out a values type, confidence scores, and doctrine-widening. Actionable residue of the [second-mind taxonomy analysis](/meta/analysis/second-mind-taxonomy-and-the-belief-gap.md). `status: proposed`.
- [Reconcile dangling routing-ledger strands into tracked work](/meta/plans/reconcile-dangling-ledger-strands.md) — a read-only `mix brain.strands` sweep listing every pending routing-ledger strand across `meta/threads/` with its reconciliation status (promoted vs. un-promoted), plus a disposition motion turning each into a todo/plan/issue or an explicit close — so deferred work stops living only inside frozen thread bodies and recurring forever in `/priorities`. Central open question: mutate the frozen ledger cell on resolution, or dedup by an `attribution.from` back-link. `status: proposed`.
- [Epistemic overlay: the four operations as a frontmatter-native graph over concepts](/meta/plans/epistemic-overlay.md) — promote the latent attestation/aggregation/inference/prescription structure to a first-class, queryable layer over existing concepts, with an integrity-checking `mix brain.graph`; explicitly bounds out atomization and strength-as-count. Reconciled 2026-07-13 against the belief-decomposition sibling plan (shared graph-algorithm core) and against repo drift (a corrected external-path corruption, moved capture-manifest target). `status: proposed`.
- [Compile the contract's Skills section from SKILL.md frontmatter](/meta/plans/compile-skills-registry-from-skill-frontmatter.md) — **operator-designated top priority** (`priority: 1`, ranked by the session-init override PR #45 built). Invert the skills-registry drift: each `SKILL.md`'s frontmatter becomes the source of truth for contract §7, rendered by the compiler with the existing `--check` gate; the hand-kept skills-registry policy shrinks to a wrapper or retires. `status: proposed`.
- [Belief decomposition as a derived analysis mode](/meta/plans/belief-decomposition-analysis-mode.md) — a read-only tool decomposing artifacts into a derived, regenerable belief graph (attestations + inferences, bipartite justifications), auditing groundedness/conflict/consensus over LLM-judged edges; only committable output is a filed `analysis` doc. Complements (does not compete with) the epistemic overlay's no-atomization storage line. `status: proposed`.
- [Auto-intake escape-rate sampling: measure the quality of what /research files](/meta/plans/auto-intake-escape-rate-sampling.md) — build `mix brain.escape_rate`, the precision-side complement to the [dedup recall probe](/meta/plans/dedup-recall-probe.md): treat the operator's post-intake editorial edits (recovered from git history, keyed on `channel:auto-intake`) as a near-zero-cost ground-truth oracle, reporting **reviewed-escape-rate** alongside an explicit **coverage** (unreviewed) fraction so silence is never counted as success. The concrete trust experiment the [tier-3/4 analysis](/meta/analysis/tier-3-4-interface-and-trust-determination.md) argues for — measured trust before scaled autonomy. `status: proposed`.

## Accepted / In progress

- [Transplant the salvageable content from the surviving unmerged claude/* branches](/meta/plans/transplant-surviving-unmerged-branches.md) — recover `main`-absent content by transplanting onto the reorganized tree (never plain-merging). Scope shrank on filing (epistemic batch + `repo-skills`/`council` ports already merged via parallel sessions): two small ports remain (Chroma product intake, `/sync-branch-with-main` skill) plus two false-orphan deletions. Closes the orphaned-branches cleanup. `status: in-progress`.
- [A /council skill: adversarial draft-PR review, gated on disposition](/meta/plans/council-skill.md) — build `.claude/skills/council/SKILL.md` implementing the operator-supplied council-round protocol (open/review/respond/close motions, gated on every finding receiving a disposition) after four bindings reconcile it with session-capture, the plan genre as distillation target, `/create-pull-request`, and a skills-registry ratification. Ratified 2026-07-13 from [council-round-suitability](/meta/analysis/council-round-suitability.md)'s integrate verdict; execution deferred. `status: accepted`.
- [Three-level documentation: plain, technical, and grounding tiers](/meta/plans/three-level-documentation.md) — opt-in per document: a plain-speak backstop block for the tutorial-mode reader, the canonical terse body unchanged, and an `implemented_by` grounding edge one abstraction below the source (path refs to code for policies and to policies for doctrine) — one canonical level plus two anchored derivations, never three parallel texts. Ratified 2026-07-13; pre-execution design findings recorded (blockquote tiers, path refs for governance targets); execution deferred. `status: accepted`.
- [Glossary single canonical overview, expansion-only bodies, and the mix brain.glossary check](/meta/plans/glossary-single-overview-and-dedup-check.md) — make each term's `description` the one canonical overview (entry-page lede + verbatim index gloss), rewrite every entry body to expansion-only, and enforce both with a new `mix brain.glossary` verifier (`descriptions` / `index sync` / content-word-containment repetition check, fail ≥ 0.72 / warn ≥ 0.55). Recreated against current `main` from the superseded, conflict-ridden PR #64 (built on the pre-rename `sb:` corpus). Executed this session: verifier + task + 12 tests, 155 term bodies rewritten (0 fails, 0 links lost), index materialized, wired into CI / pages / pre-commit, skill + hub updated. `status: accepted`.

## Done / Superseded

- [Code tutorial and generated code-map: a prose architecture tour plus a compiled function/struct glossary](/meta/plans/code-tutorial-and-code-map.md) — documented the `elixir_mind` tooling with two coupled artifacts, built in one session: the generated [`meta/code-map.md`](/meta/code-map.md) (module/function/type intent compiled from `lib/` docstrings by a new zero-dep `mix brain.codemap`, CI-`--check`-gated beside `CLAUDE.md`/`registry.md`) and the [tooling-architecture tutorial](/meta/tutorials/the-tooling-architecture.md) (a single spine doc placing every module in the parse → scan → generate → publish pipeline and linking the existing narrow tutorials). Prose authored, glossary compiled; they meet at the `@moduledoc`/`@doc`/`@typedoc`. `status: done`.
- [Collection views by date: order the governance indexes by timestamp](/meta/plans/collection-view-by-date.md) — give plans, todos, and issues a date-ordered view with the least work: entries in each collection's `index.md` are kept ordered by most recent `timestamp`, maintained at filing time; the surveyed alternatives (a generic `mix brain.recent` primitive, skill-level `by-date` dispatch, a site-side sort toggle) are recorded as deferred options. `status: done`.
- [Attribution: a frontmatter record of the ingestion event](/meta/plans/resource-attribution-property.md) — every bundle concept **and governance doc** carries a structured `attribution` map recording when it was ingested, through which channel and by whom, and why — plus, on governance docs, an append-only `from` back-link to the thread or concept it was extracted from (subsumed the elaboration `thread:` field and the hand-kept flow `lineage:` blocks, which `mix brain.lineage` now derives from `from` + `pr:`): an immutable event record orthogonal to `provenance` (content origin) and `timestamp` (last change). Ratified and fully executed same day: policy + verifier rule 8 (presence mandatory), skill writers, 265-doc git-derived backfill, `auto-intake`-tag retirement, lineage derivation, and the `mix brain.attribution` query surface. `status: done`.
- [Glossary sense disambiguation: common vs. repo-specific usage](/meta/plans/glossary-sense-disambiguation.md) — classify every glossary term by where its usage lives via a `sense` frontmatter field (`common` · `repo` · `dual`), a common-first body convention for dual entries, verifier rule 7 requiring the field on all glossary concepts, and sense-aware `/add-to-glossary` merge conventions. Proposed, ratified, and implemented same session. `status: done`.
- [Derived dev history: a per-PR progress view compiled from the merge graph](/meta/plans/derived-dev-history.md) — the hand-kept log's convenience without its craft: `mix brain.dev_history` derives [`meta/dev-history.md`](/meta/dev-history.md) (one bulleted section per merged PR, all three history eras handled) entirely from the first-parent merge graph, with a lag-tolerant `--check` in CI and the pre-commit hook, and a deploy-time re-derivation so the Pages copy is always current. `status: done`.
- [Rename the brain: second-brain → elixir-mind (including sb: → em: id migration)](/meta/plans/rename-second-brain-to-elixir-mind.md) — ratified 2026-07-13; Phase 1 execution handed to a fresh session. Rename the repo and all associated naming to **elixir-mind** (retargeted from an earlier `elixir-brain`), and migrate the stable-id prefix `sb:` → `em:`. Two atomic PRs: workstream A (Elixir app/module namespace, hook, maintained docs, regenerated artifacts, frozen-thread `<routes>` path refs) and workstream B (a tail-preserving, bundle-wide `sb:X → em:X` prefix swap across the minted ids + tokens, code/verifier flip, stable-identity policy amendment), plus an operator repo-rename on GitHub. The `em:` prefix mirrors the display name but stays opaque in the model. Six decisions, including a name-prose vs. code-identifier split (Decision 6) that sweeps live `SecondBrain`/`lib/second_brain` refs across ~37 doc surfaces. Token counts re-measured at execution (the corpus roughly doubled after a main-merge). Both PRs merged 2026-07-14 (name rename #86; id migration this PR). `status: done`.
- [Flow lineage: frontmatter provenance + a derived flowchart](/meta/plans/flow-lineage-index.md) — every flow doc carries a canonical `lineage:` frontmatter block (`analysis → plan → thread → PR`); `mix brain.lineage` materializes a per-doc blockquote and derives the cross-flow flowchart index [`meta/flows/lineage.md`](/meta/flows/lineage.md) (Mermaid + table), `--check`-gated in CI and the pre-commit hook. All eight flow docs retrofitted (feature lineage, not doc-authorship). The flow-scoped precursor of the epistemic-overlay's typed lineage edges. `status: done`.
- [Auto-intake featured /research items: move the human from the intake gate to the editorial surface](/meta/plans/auto-intake-featured-research.md) — auto-run `/intake` on the items `/research` featured, relocating the operator's role from a pre-intake gate (judged artificial — you can't assess filing/dup until a thing is in the system) to post-intake editorial work; bounded to the known tree, preferring update-in-place on `relates to em:` hints, tagged `auto-intake`, deferring new-top-level-domain items. **Fork A** (gated behind PR #50's tier-1 `/intake` fix); shipped as the `/research` §6 step with flow-doc + policy mirror. `status: done`.
- [Code review toolchain hardening](/meta/plans/code-review-toolchain-hardening.md) — the 2026-07-11 top-down review + staleness audit (findings kept as background) and its executed hardening: two-directional `materialize` (orphan removal; issue resolved), verifier rule 6 gating `verified` to statement types, and the five-item hygiene batch — with an explicit out-of-scope list. `status: done`.
- [Dedup recall probe: gold set + `mix brain.dedup_probe`](/meta/plans/dedup-recall-probe.md) — a zero-dependency, offline eval scoring the lexical search layer against an id-keyed gold set of natural-phrasing dedup queries (bands for negatives and quarantined cases, gold pairs harvested at intake, non-gating CI report); the quantified trigger for tier-2 embedding dedup and the substrate for later corpus-maintenance evals. `status: done` — instrument (gold set, task, tests, CI report, baseline) **and** the tier-1 `/intake` synonym-expansion + automated gold-harvest all shipped.
- [Retire the hand-kept chronological logs (log.md) in favor of git history](/meta/plans/retire-hand-kept-logs.md) — removed root `log.md`, `meta/log.md`, and `inbox/log.md` and every policy/skill mandate to maintain them; git's true-merge commit graph is the single provenance layer. The generated route-tagged excerpt logs stay. Filed, ratified, and executed same day. `status: done`.
- [The daily read: a cross-domain synthesis lede for /research digests](/meta/plans/research-daily-read-synthesis.md) — add a `## The read` section at the top of each digest: a short, `em:`-id-grounded perspective reading the day's selections as a set against the brain's standing concerns, connecting threads across domains. Proposed, ratified, and implemented same session. `status: done`.
- [The flows genre + formal scenario testing](/meta/plans/flows-genre-and-scenario-testing.md) — establish a `meta/flows/` genre (per-flow touch-sequence docs) backed by ExUnit scenario tests over each flow's deterministic spine; collapse the capture docs and build the `session-capture` + `intake` flows. `status: done`.
